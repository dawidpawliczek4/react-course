import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "dark",
    background: {
      default: "#1E293B",
      paper: "rgb(15 23 42)"
    },
  },
});

export default theme;
